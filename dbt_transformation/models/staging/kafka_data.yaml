config-version: 2

sources:
  - name: kafka_source  # Rename if needed for clarity
    config-version: 2
    config:
      sources:
        - name: kafka
          adapter: kafka
          brokers:
            - localhost:9092
          topics:
            - name: postgres.public.emission
